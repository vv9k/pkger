language: rust
rust:
  - stable
addons:
  apt:
    packages:
      - docker
install:
  - sudo apt -y install docker
  - sudo dockerd -H tcp://0.0.0.0:2376 --pidfile /var/run/docker_api.pid &
script:
  - RUST_LOG=pkger=trace cargo run -- -d http://0.0.0.0:2376 -c example/conf.toml pkger
